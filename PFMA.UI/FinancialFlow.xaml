<UserControl x:Class="PFMA.Interface.FinancialFlow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:local="clr-namespace:PFMA.Interface"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="569">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <lvc:CartesianChart Series="{Binding IncomeSeries}" XAxes="{Binding XAxes}" LegendPosition="Hidden"/>
        <lvc:CartesianChart Grid.Row="1" Series="{Binding ExpenseSeries}" XAxes="{Binding XAxes}" LegendPosition="Hidden" />
    </Grid>
</UserControl>
